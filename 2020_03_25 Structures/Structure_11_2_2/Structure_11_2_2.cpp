// demonstrate defining a structure

#include <iostream>
#include <string>
using namespace std;

struct Student  // Student is the name of the structure, also called structure tag
{
    int studentID;      // member or field called studentID
    string studentName; // member or field called studentName
    char yearInSchool;  // member or field called yearInSchool;
    double gpa;         // member or field called gpa
};

int main(int argc, char** argv) {

    return 0;
}

